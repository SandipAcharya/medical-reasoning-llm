# ─────────────────────────────────────────────────────────────
#  medical-reasoning-llm — Python dependencies
#  Pin major versions; patch versions float for security fixes.
# ─────────────────────────────────────────────────────────────

# ── Core ML ──────────────────────────────────────────────────
torch>=2.1.0,<2.4.0
transformers>=4.43.0,<4.50.0
tokenizers>=0.19.0
accelerate>=0.30.0
datasets>=2.19.0
peft>=0.11.0
trl>=0.9.0
bitsandbytes>=0.43.0          # 4-bit QLoRA quantization

# ── Evaluation ───────────────────────────────────────────────
rouge-score>=0.1.2
nltk>=3.8.1
evaluate>=0.4.2
scikit-learn>=1.4.0

# ── Experiment Tracking (optional, comment out if not needed) ─
# wandb>=0.17.0

# ── Config & Utilities ───────────────────────────────────────
omegaconf>=2.3.0
pyyaml>=6.0.1
python-dotenv>=1.0.0
tqdm>=4.66.0
rich>=13.7.0                  # Beautiful terminal output
typer>=0.12.0                 # CLI framework for scripts

# ── Serialization ─────────────────────────────────────────────
safetensors>=0.4.3
huggingface_hub>=0.23.0

# ── Notebooks ─────────────────────────────────────────────────
jupyter>=1.0.0
ipywidgets>=8.1.0
matplotlib>=3.8.0
seaborn>=0.13.0
pandas>=2.2.0

# ── Dev / Testing ─────────────────────────────────────────────
pytest>=8.2.0
pytest-cov>=5.0.0
black>=24.4.0
ruff>=0.4.0
mypy>=1.10.0